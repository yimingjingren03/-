//最大值
((a<b?b:a)<(a<c?c:a)):
//最小值

//中间值
a<b?(a>c?a:(c<b?c:b));:(a<c?a:(b>c?b:c))

//最大字符串
strcmp(a,b)>0?(strcmp(a,c)>0?a:c):(strcmp(b,c)>0?b:c)
//最小字符串
strcmp(a,b)<0?(strcmp(a,c)<0?a:c):(strcmp(b,c)<0?b:c)]
//中间字符串
strcmp(a,b)>0?(strcmp(a,c))

//还没完，明天来。
